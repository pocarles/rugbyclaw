name: Deploy Proxy Worker

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "worker/**"
      - ".github/workflows/deploy-worker.yml"

concurrency:
  group: deploy-worker
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x
          cache: npm
          cache-dependency-path: worker/package-lock.json

      - name: Install worker deps
        working-directory: worker
        run: npm ci

      - name: Prepare Wrangler config
        working-directory: worker
        env:
          CF_KV_RATE_LIMITS_ID: ${{ secrets.CF_KV_RATE_LIMITS_ID }}
        run: |
          if [ -z "$CF_KV_RATE_LIMITS_ID" ]; then
            echo "Missing secret: CF_KV_RATE_LIMITS_ID"
            exit 1
          fi

          cp wrangler.toml wrangler.ci.toml
          sed -i "s/id = \"your-kv-namespace-id\"/id = \"$CF_KV_RATE_LIMITS_ID\"/" wrangler.ci.toml

      - name: Set API_SPORTS_KEY secret
        working-directory: worker
        env:
          API_SPORTS_KEY: ${{ secrets.API_SPORTS_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "Missing secret: CLOUDFLARE_API_TOKEN"
            exit 1
          fi
          if [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "Missing secret: CLOUDFLARE_ACCOUNT_ID"
            exit 1
          fi
          if [ -z "$API_SPORTS_KEY" ]; then
            echo "Missing secret: API_SPORTS_KEY"
            exit 1
          fi

          printf "%s" "$API_SPORTS_KEY" | npx wrangler secret put API_SPORTS_KEY --config wrangler.ci.toml --name rugbyclaw-proxy

      - name: Deploy Worker
        working-directory: worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx wrangler deploy --config wrangler.ci.toml

